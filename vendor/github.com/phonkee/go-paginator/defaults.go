package paginator

const(
	DEFAULT_PAGE = 1
	DEFAULT_PAGE_PARAM = "page"
	DEFAULT_PER_PAGE = 10
	DEFAULT_PER_PAGE_PARAM = "limit"

	// context keys
	CONTEXT_ON_REQUEST_REQUEST = 1
)

var (
	DEBUG = true
)